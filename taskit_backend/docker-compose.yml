version: "3.8"

services:
  backend:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis
    env_file:
      - .env

  redis:
    image: redis
    ports:
      - "6379:6379"
